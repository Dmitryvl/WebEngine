@using WebEngine.Web.ViewModels.Product
@model WebEngine.Web.ViewModels.Product.ProductListView


<div id="sidebar" class="col-xs-6 col-sm-3 sidebar-offcanvas" role="navigation">
	@*@Component.Invoke("CategoryList")*@
	TODO: filter
</div>
<div class="col-xs-12 col-sm-9">

		@foreach (ProductView product in Model.Products)
		{
			<p>
				@product.CompanyName @product.Name
				<br />
				@foreach (var item in product.Properties)
				{
					<span>@item.Value @item.SizeValue</span>
				}
			</p>
		}
	
</div>